name = "wardyati-servers"          # ← changed to match what your CI expects (removes the yellow warning)
main = "src/index.js"
compatibility_date = "2025-12-01"

[durable_objects]
bindings = [
  { name = "SNIPE_OBJECT", class_name = "SnipeObject" }
]

[[migrations]]
tag = "v1"
new_sqlite_classes = [ "SnipeObject" ]
